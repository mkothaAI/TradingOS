[pytest]
# Collect tests from top-level tests and package-level tests under trading_os_v1/tests
testpaths = tests trading_os_v1/tests
# Prevent recursing into the package implementation directory which contains another tests/ tree
norecursedirs = trading_os_v1/trading_os_v1
